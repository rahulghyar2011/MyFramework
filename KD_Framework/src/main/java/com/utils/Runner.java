package com.utils;


import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.IOException;
import java.util.List;
import java.util.Properties;

import org.apache.log4j.Logger;

import com.keyword.Constants;

public class Runner {

	public static void main(String[] args)
	{
		  /*  String configpath="E:\\KeywordData\\config.properties";
		  Constants.log=Logger.getLogger(Runner.class);
		  Constants.property=new Properties();
		try {
			//Constants.fis= new FileInputStream(args[0]);
			Constants.fis= new FileInputStream(configpath);
		} catch (FileNotFoundException e) {
			Constants.log.error("File Not Found:"+configpath);
			e.printStackTrace();
		}
		try {
			Constants.property.load(Constants.fis);
			System.out.println("loaded succefully..");
		} catch (IOException e) {
			Constants.log.error("Error in loading properties file");
			e.printStackTrace();
		}
		for(int i=0;i<str.length;i++){
		 str[i]= Constants.property.getProperty(key);
		}
		List<String> list=Constants.property.getProperty(key)
		System.out.println("MAstersheetPathIs: "+ configread);
		
	
		
*/
	}

}
